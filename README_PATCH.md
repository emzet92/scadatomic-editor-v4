# SCADAtomic Design System — Shadows / Elevation patch

Base: `scadatomic-editor-design-system-radius-source.zip`

Adds structured Shadow / Elevation tokens with stable UUID references, a design-system workspace, local/token property control, central renderer resolution, safe detach on delete, and support for Button, Container, Modal, Image, Navigation and Text.

Validation: TypeScript 6 OK, ESLint changed files OK, shadow resolver/count/detach smoke OK, `git apply --check` OK.
